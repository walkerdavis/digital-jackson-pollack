services:
  - type: web
    name: digital-jackson-pollack
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn src.server:app --bind 0.0.0.0:$PORT
    envVars:
      - key: PYTHON_VERSION
        value: "3.11"